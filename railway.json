{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --workspaces --include-workspace-root --no-audit --no-fund && npm run build --workspace @actualizecrm/db && npm run build --workspace @actualizecrm/worker"
  },
  "deploy": {
    "startCommand": "npm run db:deploy && npm run worker:start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
